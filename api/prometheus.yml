global:
  scrape_interval: 5s # How often to scrape targets

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090'] # Prometheus scrapes its own metrics

  - job_name: 'lolgraph'
    static_configs:
      - targets: ['lolgraph:8080'] 
